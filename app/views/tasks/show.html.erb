<h2><%= @task.title %></h2>

<p><%= @task.completed %></p>

<%= link_to 'Edit task', edit_task_path(@task.id) %>
<%= link_to 'Back to all tasks', tasks_path %>
<%= link_to 'Remove a task', destroy_task_path(@task.id) %>
